#include "WorldSettings.h"

// Only one definition in the project;
const sf::Vector2i WorldSettings::windowSize(WorldSettings::screenSizeX, WorldSettings::screenSizeY);
const sf::Vector2i WorldSettings::mapSize(WorldSettings::mapSizeX, WorldSettings::mapSizeY);
const sf::Vector2i WorldSettings::minSectionSize(WorldSettings::minSectionX, WorldSettings::minSectionX);
const sf::Vector2i WorldSettings::maxSectionSize(WorldSettings::maxSectionX, WorldSettings::maxSectionY);